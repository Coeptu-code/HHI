# Onboarding Flow

1. Agent logs in
2. Agent creates a questionnaire link (token)
3. Client opens `/intake/<token>/`
4. Client completes intake form + consents
5. System stores submission + answers
6. System runs deterministic gap scoring
7. Agent reviews pre-call summary and referrals
