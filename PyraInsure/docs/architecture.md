# Architecture

Standalone Django app using server-rendered templates.

- `apps/`: domain apps (agents, intake, gap scoring, referrals, etc.)
- `templates/`: HTML templates (Black Dashboard-ready structure)
- `static/`: CSS/JS/images (placeholder folders)
